# Binaries and archives
ngrok
ngrok.tgz
ngrok.yml

# Log files
fastapi_startup.log

# Frontend specific ignores
frontend/.env.local
frontend/node_modules/
frontend/.next/

# Backend generated data/cache
chunks/
ocr_txt/
raw/
**/__pycache__/

# Poetry lock file (usually committed, but ignoring for now as per request)
poetry.lock

.env
.env.local
