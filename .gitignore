# Environment files (may contain sensitive IPs and keys)
.env
.env.*
!.env.example

# Frontend specific ignores
frontend/.env.local
frontend/node_modules/
frontend/.next/

# Backend generated data/cache
chunks/
ocr_txt/
raw/
**/__pycache__/

# Poetry lock file (usually committed, but ignoring for now as per request)
poetry.lock

# Log files
**/*.log
fastapi_startup.log
server.log

# Process ID files
.backend.pid
.frontend.pid

# Binaries and archives
ngrok
ngrok.tgz

# ngrok configuration (may contain auth tokens)
ngrok.yml